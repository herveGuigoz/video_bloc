name: video_bloc
description: Bloc video state management
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.1.6
  rxdart: ^0.27.1
  freezed_annotation: ^0.14.2
  freezed: ^0.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.5

flutter: